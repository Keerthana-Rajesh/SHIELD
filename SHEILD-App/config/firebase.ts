type FirebasePlaceholder = null;

// Firebase SDK packages are not installed in this project. Keep this module
// compile-safe until a real Firebase integration is added.
const app: FirebasePlaceholder = null;
const storage: FirebasePlaceholder = null;

export { storage };
export default app;
